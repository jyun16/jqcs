import t from"../../jq/jqc.min.js";const s=console.log;await t.import("modal");let l=null;t.define("j-rich-select",{html:`<div class="tags" @click='toggleOpts()' '{{ !attrs }}'>
	{% for k in p.vals %}
		<div class="tag">
			<div class="lbl">{{ p.opts[k] }}</div>
			<div class="rm material-symbols-outlined" @click="rmVal('{{ k }}')">close</div>
		</div>
	{% end %}
	<modal>
		<div class="opts">
			<input type='text' @typed='filter(e)' @stop="@stop" value='{{ p.filter }}'></input>
			<ul>
				{% for k, v in p.opts %}
					{% if (!p.vals.includes(k) && v.includes(p.filter)) %}
						<li @click="select('{{ k }}')">{{ v }}</li>
					{% end %}
				{% end %}
			</ul>
		</div>
	</modal>
</div>`,css:`input {
  width: 100%;
  padding: 10px;
  border: 2px solid #ddd;
  border-radius: 5px;
}
input:focus {
  border-color: #0099ff;
  outline: none;
}

.tags {
  display: flex;
  position: relative;
  flex-wrap: wrap;
  align-items: center;
  gap: 0.5em;
  width: 100%;
  height: 2.6rem;
  padding: 0 0.5rem;
  background-color: white;
  border: 2px solid #ddd;
  border-radius: 5px;
  cursor: pointer;
  margin-bottom: 0.5rem;
}
.tags .tag {
  display: flex;
  align-items: center;
  background: #666;
  padding: 0 0.5rem;
  border-radius: 4px;
  user-select: none;
  height: 2rem;
  cursor: default;
}
.tags .tag .lbl {
  transform: translateY(-1px);
}
.tags .tag .rm {
  transform: translateX(4px);
  font-size: 1.2rem;
  border: none;
  cursor: pointer;
}

.opts {
  padding: 0.3rem;
  border: 1px solid #ccc;
  background-color: #666;
}
.opts ul {
  overflow-y: auto;
  max-height: var(--option-height, 300px);
  width: 100%;
  margin: 0.3rem 0;
  padding: 0;
  list-style: none;
}
.opts ul li {
  padding: 0.3em 0.5em;
  cursor: pointer;
  user-select: none;
}
.opts ul li:hover {
  background: #eee;
}`,globalCss:"",p:{vals:[],opts:{},filter:"",option_height:""},init(){this.render()},postRender(){l=t.bind("modal")},methods:{toggleOpts(){l.toggle()},filter(n){this.p.filter=n.target.value,this.render();const e=this.find("input").el(0);e.focus();const i=e.value;e.setSelectionRange(i.length,i.length)},select(n){this.p.vals.includes(n)||(this.p.vals.push(n),this.p.filter="",this.render())},rmVal(n){this.p.vals=this.p.vals.filter(e=>e!==n),this.render()}}});export default!0;
