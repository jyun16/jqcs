import n from"../../jq/jqc.min.js";const i=console.log;n.define("j-rich-select",{html:`<div class="items" @click='open'>
	{% for v in val %}
		<div class="item">
			<span>{{ opts[v] }}</span>
			<span class="icon close" @click='rmItem(e)' value='{{ v }}'>close</span>
		</div>
	{% end %}
</div>
<j-dialog p-btn.close="false" p-fit="true" @typed='search'>
	<j-search></j-search>
	<div class="options" @click='select(e)'>
		{% for v, l in opts %}
			{% if !val.includes(v) %}
				<div class="option" value='{{v}}'>{{l}}</div>
			{% end %}
		{% end %}
	</div>
</j-dialog>`,css:`.items {
  display: flex;
  align-items: center;
  gap: 0.3rem;
  height: 2.5rem;
  padding: 0 0.5rem;
  background: #fff;
  border-radius: 5px;
  user-select: none;
}
.items .item {
  display: flex;
  align-items: center;
  height: 1.8rem;
  padding: 0.1rem 0.3rem 0.2rem 0.4rem;
  color: #fff;
  background: #5e6264;
  border-radius: 0.2rem;
  cursor: default;
}
.items .item .close {
  cursor: pointer;
}

.options {
  display: flex;
  flex-direction: column;
}
.options :not(:first-child) {
  border-top: 1px solid #fff;
}
.options .option {
  display: flex;
  align-items: center;
  height: 2.5rem;
  user-select: none;
  cursor: pointer;
}
.options .option:hover {
  color: #fff;
  background: #eee;
}`,globalCss:"",p:{name:"",val:["hoge"],opts:{hoge:"HOGE",fuga:"FUGA",rselect1:"R-SELECT 1",rselect2:"R-SELECT 2"}},init(){this.render();const e=n.bind("j-dialog",this),t=n.bind("j-search",this);t.render(),this.$dia=e,this.$filter=t,this.$dia.open(this)},methods:{open(){this.$dia.open(this)},rmItem(e){e.stopPropagation(),i("rm",e.target.getAttribute("value"))},search(e){i(this.$filter.p.val)},select(e){i("sel",e.target.getAttribute("value")),this.$dia.close()}}});export default!0;
