import i from"../../jq/jqc.min.js";i.define("j-checkbox",{html:`{% for v, l in opts %}
	<label>
		<input type='checkbox' value='{{ v }}' @change='change(e)' '{{ !attrs }}' {% if val.includes(v) %}checked{% end %}>{{ l }}</input>
	</label>
{% end %}`,css:`this {
  display: flex;
  flex-wrap: wrap;
  gap: 0 1em;
  border: none;
}
this label {
  display: flex;
  align-items: center;
  gap: 0 0.5em;
  position: relative;
  cursor: pointer;
  user-select: none;
}
this label::before {
  width: 14px;
  height: 14px;
  border-radius: 3px;
  border: 2px solid #ddd;
  content: "";
}
this label:has(:checked)::after {
  position: absolute;
  top: 5px;
  left: 6px;
  transform: rotate(45deg);
  width: 4px;
  height: 9px;
  border: solid #0099ff;
  border-width: 0 3px 3px 0;
  content: "";
}
this input {
  display: none;
}`,globalCss:"",p:{name:"",val:[]},methods:{change(t){const e=t.target;let n=this.p.val;e.checked?n.push(e.value):this.p.val=n.filter(l=>l!=e.value)}}});export default!0;
