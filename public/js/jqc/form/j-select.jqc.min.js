import l from"../../jq/jqc.min.js";l.define("j-select",{html:`<select name='{{ name }}' '{{ !attrs }}'>
	{% for v, l in opts %}
		<option value='{{ v }}' '{% if v == val %}selected{% end %}'>{{ l }}</option>
	{% end %}
</select>`,css:`this {
  display: inline-flex;
  align-items: center;
  position: relative;
}
this select {
  appearance: none;
  padding: 10px;
  min-width: 230px;
  cursor: pointer;
}
this::after {
  position: absolute;
  right: 15px;
  width: 10px;
  height: 7px;
  background-color: #535353;
  clip-path: polygon(0 0, 100% 0, 50% 100%);
  content: "";
  pointer-events: none;
}`,globalCss:"",p:{name:"",val:""},methods:{change(t){const e=t.target;let n=this.p.val;e.checked?n.push(e.value):this.p.val=n.filter(i=>i!=e.value)}}});export default!0;
