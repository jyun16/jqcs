import t from"../../jq/jqc.min.js";t.define("j-radio",{html:`{% for v, l in opts %}
	<label>
		<input type='radio' value='{{ v }}' @change='change(e)' '{{ !attrs }}' '{% if val == v %}checked{% end %}'>{{ l }}</input>
	</label>
{% end %}`,css:`this {
  display: flex;
  flex-wrap: wrap;
  gap: 0 1em;
  border: none;
}
this label {
  display: flex;
  align-items: center;
  gap: 0 0.5em;
  position: relative;
  cursor: pointer;
  user-select: none;
}
this label::before, this label::after {
  border-radius: 50%;
  content: "";
}
this label::before {
  width: 18px;
  height: 18px;
  border: 2px solid #ddd;
  box-sizing: border-box;
}
this label::after {
  position: absolute;
  top: 50%;
  left: 9px;
  transform: translate(-50%, -50%);
  width: 9px;
  height: 9px;
}
this label:has(:checked)::after {
  background-color: #0099ff;
}
this input {
  display: none;
}`,globalCss:"",p:{name:"",val:"",opts:{}},methods:{change(e){const n=e.target;n.checked&&(this.p.val=n.value)}}});export default!0;
