import i from"../../jq/jqc.min.js";i.define("j-mselect",{html:`<select name='{{ p.name }}' '{{ !attrs }}' multiple>
	{% for v, l in p.opts %}
		<option value='{{ v }}' '{% if p.val.includes(v) %}selected{% end %}'>{{ l }}</option>
	{% end %}
</select>`,css:`this {
  display: inline-flex;
  align-items: center;
  position: relative;
}
this::after {
  position: absolute;
  right: 15px;
  width: 10px;
  height: 7px;
  background-color: #535353;
  clip-path: polygon(0 0, 100% 0, 50% 100%);
  content: "";
  pointer-events: none;
}
this select {
  appearance: none;
  padding: 10px;
  border: 2px solid #ddd;
  border-radius: 5px;
  background-color: #fff;
  min-width: 230px;
  font-size: 1em;
  cursor: pointer;
}
this select:focus {
  border-color: #0099ff;
  outline: none;
}`,globalCss:"",p:{name:"",val:[]},methods:{change(t){const n=t.target;let e=this.p.val;n.checked?e.push(n.value):this.p.val=e.filter(o=>o!=n.value)}}});export default!0;
